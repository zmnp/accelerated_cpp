/* 0-1. What does the following statement do?
 * 3 + 4;
 */

int main() {
  // 3 + 4 is an expression, the result of which is 7
  // but it does nothing because semicolon after this expression discards the result
  3 + 4;
  return 0;
}
